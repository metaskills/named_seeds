sudo: false
cache: bundler
rvm:
  - 2.0.0
  - 2.1.5
  - 2.2.1
install:
  - gem install bundler
  - bundle --version
  - bundle install
before_script:
  - bundle exec appraisal install
script:
  - bundle exec appraisal rake test
